let n = 6;

for (let i = 1; i <= n; i++) {
    let start = (i % 2 === 1) ? 1 : 0;
    let row = "";
    
    for (let j = 1; j <= i; j++) {
        row += start + " ";
        start = start === 1 ? 0 : 1;
    }
    console.log(row.trim());
}
